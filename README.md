# Student Management API

A RESTful API built with FastAPI + SQLite to manage student records (CRUD) and provide filtering by major and GPA threshold.

## Setup

### 1) Create and activate a virtual environment

**Windows (PowerShell):**
```powershell
python -m venv .venv
.venv\Scripts\Activate.ps1